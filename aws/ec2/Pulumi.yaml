name: ec2
description: A minimal AWS Python Pulumi program that spins up an EC2 instance
runtime:
  name: python
  options:
    toolchain: uv
config:
  pulumi:tags:
    value:
      pulumi:template: aws-python
