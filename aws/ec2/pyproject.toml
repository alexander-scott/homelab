[project]
name = "ec2"
version = "0.1.0"
description = "Pulumi project to spin up an EC2 machine on AWS"
readme = "README.md"
dependencies = [
    "pulumi>=3.256.0,<4.0.0",
    "pulumi-aws>=7.40.0,<8.0.0",
    "pulumi-command>=1.1.3",
]
